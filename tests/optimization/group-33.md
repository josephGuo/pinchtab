# Group 33: srcdoc iframe

### 33.1 Interact with an inline-content iframe
Navigate to `http://fixtures/iframe-srcdoc.html`. The iframe's content is inlined via `srcdoc`; it has no `src` URL. Scope into the iframe, fill the input with `srcdoc`, submit, and verify.

**Verify**: Scoped snapshot contains `INLINE_RECEIVED_SRCDOC`.

---

