# Group 31: Nested iframes (3 levels deep)

### 31.1 Click a button in the deepest frame
Navigate to `http://fixtures/iframe-nested.html`. The page has nested iframes three levels deep — drill through the frame hops to reach the innermost button and click it. Verify via a scoped observation before resetting to the main frame.

**Verify**: Scoped snapshot contains `DEEP_CLICKED=YES_LEVEL_3`.

---

